.small-6.small-offset-3.columns
  %fieldset
    %legend Editing #{@localization.name}

    = form_for @localization, html: {role: 'form'} do |f|
      .row
        = f.label :name
        = f.text_field :name
      .row
        = f.submit 'Submit', class: 'button tiny success'
