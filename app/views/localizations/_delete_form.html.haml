- submit_btn_css ||= 'button tiny alert full'
= form_tag localization do
  %input{:name => "_method", :type => "hidden", :value => "delete"}/
  = submit_tag 'Destroy', onclick: "return confirm('Are you sure?')", class: submit_btn_css
