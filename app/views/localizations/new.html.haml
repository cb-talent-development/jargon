.small-6.small-offset-3.columns
  %fieldset
    %legend New Localization

    = form_for @localization, html: {role: 'form'} do |f|
      .row
        = f.label :name
        = f.text_field :name
      .row
        = f.submit 'Submit', class: 'button tiny success right'
